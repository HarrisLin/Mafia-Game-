25.1.2015
Testing Charter for Game Engine

a charter outlining tests to add for each character
based on Junit for BDD

Target:
lynch & vote

lynch();
1) character_vote_map // list of all players
2) lynchVote - ifNight = 0, ifDay = reset, endofDay -> calculate
Returns:
person with most votes is lynched
player.kill()
resetVote();

vote():
1) ex. elly.vote(derek) -> votes derek
2) --> mayor override vote() where numVote = 2;

other stuff added:
moved vote() to Player class
made graveyard for dead players